import React from "react";

const FirstStep = () => {
  return (
    <div>
      React.js, often referred to as React, is a JavaScript library for building
      user interfaces (UIs). Developed and maintained by Facebook (now Meta) and
      a community of individual developers and companies, React allows you to
      create reusable UI components. It’s widely used for building single-page
      applications (SPAs) and dynamic web interfaces.
    </div>
  );
};

export default FirstStep;
